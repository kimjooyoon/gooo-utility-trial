# Gooo Utility Trial

An independent, fail-closed user-trial packet for measuring whether a released
Gooo human-readable evidence dossier helps people make two exact decisions:
OpenTofu drift judgment and resuming a Gooo `UNKNOWN` frontier.

The bootstrap is intentionally limited to this README, the license, and the
ignore file. Implementation is introduced through a pull request and verified
by GitHub Actions. The canonical packet contains no user receipts, so its
utility result is `UNKNOWN` until independent sessions are actually recorded.

